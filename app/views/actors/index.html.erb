<h1>Listing actors</h1>

<table>
  <thead>
    <tr>
      <th>Stagename</th>
      <th>Realname</th>
      <th>Dob</th>
      <th>Url</th>
      <th>Residence</th>
      <th>Age</th>
      <th>Other</th>
      <th>Occupations</th>
      <th>Numberofchildren</th>
      <th>Movie</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @actors.each do |actor| %>
      <tr>
        <td><%= actor.stagename %></td>
        <td><%= actor.realname %></td>
        <td><%= actor.dob %></td>
        <td><%= actor.url %></td>
        <td><%= actor.residence %></td>
        <td><%= actor.age %></td>
        <td><%= actor.other %></td>
        <td><%= actor.occupations %></td>
        <td><%= actor.numberofchildren %></td>
        <td><%= actor.movie %></td>
        <td><%= link_to 'Show', actor %></td>
        <td><%= link_to 'Edit', edit_actor_path(actor) %></td>
        <td><%= link_to 'Destroy', actor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Actor', new_actor_path %>
